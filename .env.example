# --- API ASPHALT ---
PORT=4000
APP_URL=http://localhost:5173

# Commission plateforme (0.1 = 10 %)
ASPHALT_FEE_PCT=0.1

# --- Persistance PostgreSQL (vide = store en mémoire / démo) ---
# DATABASE_URL=postgresql://user:password@localhost:5432/asphalt

# --- Stripe Connect (laisser vide = mode mock) ---
# STRIPE_SECRET_KEY=sk_test_xxx

# --- Prestataire KYC : Stripe Identity / Onfido / Veriff (vide = mock) ---
# KYC_API_KEY=xxx

# --- Front : URL de l'API consommée par le SPA ---
VITE_API_URL=http://localhost:4000/api
